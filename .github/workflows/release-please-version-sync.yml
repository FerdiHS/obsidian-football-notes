name: Sync Release Please Version Metadata

on:
    pull_request_target:
        types:
            - opened
            - reopened
            - synchronize
            - ready_for_review
        branches:
            - main

permissions:
    contents: write
    pull-requests: write

jobs:
    sync-version-metadata:
        if: >
            github.event.pull_request.base.ref == 'main' &&
            github.event.pull_request.head.repo.full_name == github.repository &&
            startsWith(github.event.pull_request.head.ref, 'release-please--branches--main') &&
            contains(github.event.pull_request.body || '', 'This PR was generated with [Release Please]')
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: Generate GitHub App token
              id: app-token
              uses: actions/create-github-app-token@v2
              with:
                  app-id: ${{ vars.RELEASE_PLEASE_APP_ID }}
                  private-key: ${{ secrets.RELEASE_PLEASE_APP_PRIVATE_KEY }}
                  permission-contents: write
                  permission-pull-requests: write

            - name: Confirm Release Please bot authored the PR
              id: bot-check
              env:
                  PR_AUTHOR: ${{ github.event.pull_request.user.login }}
                  APP_SLUG: ${{ steps.app-token.outputs.app-slug }}
              run: |
                  if [ "$PR_AUTHOR" = "${APP_SLUG}[bot]" ]; then
                    echo "is_release_bot=true" >> "$GITHUB_OUTPUT"
                    exit 0
                  fi

                  echo "is_release_bot=false" >> "$GITHUB_OUTPUT"
                  echo "Skipping version metadata sync because this PR was not authored by the Release Please bot."

            - uses: actions/checkout@v4
              if: ${{ steps.bot-check.outputs.is_release_bot == 'true' }}
              with:
                  ref: ${{ github.event.pull_request.head.ref }}
                  token: ${{ steps.app-token.outputs.token }}
                  fetch-depth: 0

            - name: Use Node.js 20
              if: ${{ steps.bot-check.outputs.is_release_bot == 'true' }}
              uses: actions/setup-node@v4
              with:
                  node-version: 20.x
                  cache: npm

            - run: npm ci
              if: ${{ steps.bot-check.outputs.is_release_bot == 'true' }}
            - run: npm run version:sync
              if: ${{ steps.bot-check.outputs.is_release_bot == 'true' }}
            - run: npm run version:check
              if: ${{ steps.bot-check.outputs.is_release_bot == 'true' }}

            - name: Commit version metadata changes
              if: ${{ steps.bot-check.outputs.is_release_bot == 'true' }}
              env:
                  BRANCH: ${{ github.event.pull_request.head.ref }}
              run: |
                  if git diff --quiet -- manifest.json versions.json; then
                    echo "No release metadata changes were needed."
                    exit 0
                  fi

                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  git add manifest.json versions.json
                  git commit -m "chore: sync release metadata"
                  git push origin HEAD:"$BRANCH"
